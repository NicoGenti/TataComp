---
phase: quick
plan: 260331-s2e
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Timeline.tsx
  - src/pages/Magic.tsx
  - src/pages/Secret.tsx
  - src/index.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "Click destro su qualsiasi immagine non mostra 'Salva immagine con nome'"
    - "Trascinare un'immagine non la sposta fuori dal browser"
  artifacts:
    - path: "src/components/Timeline.tsx"
      provides: "img con onContextMenu + onDragStart bloccati e draggable=false"
    - path: "src/pages/Magic.tsx"
      provides: "img con onContextMenu + onDragStart bloccati e draggable=false"
    - path: "src/pages/Secret.tsx"
      provides: "img con onContextMenu + onDragStart bloccati e draggable=false"
    - path: "src/index.css"
      provides: "Regola CSS img { user-select: none; -webkit-user-drag: none }"
  key_links:
    - from: "src/index.css"
      to: "tutte le <img>"
      via: "CSS globale"
      pattern: "img\\s*\\{"
---

<objective>
Impedire il salvataggio casuale delle foto tramite click destro ("Salva immagine") e drag & drop, aggiungendo handler `onContextMenu` / `onDragStart` + attributo `draggable="false"` a tutti gli `<img>` del sito, e una regola CSS globale.

Purpose: Protezione di base per fotografie private in un sito regalo romantico.
Output: 3 file TSX aggiornati + regola CSS aggiuntiva in index.css.
</objective>

<execution_context>
@C:/Users/NicolasGentilucci/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/NicolasGentilucci/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/components/Timeline.tsx
@src/pages/Magic.tsx
@src/pages/Secret.tsx
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Blocca click destro e drag su tutte le immagini</name>
  <files>src/components/Timeline.tsx, src/pages/Magic.tsx, src/pages/Secret.tsx</files>
  <action>
    Aggiungere tre attributi a ogni elemento `<img>` nei tre file:
    ```
    onContextMenu={(e) => e.preventDefault()}
    onDragStart={(e) => e.preventDefault()}
    draggable={false}
    ```

    **Timeline.tsx** — riga 63, `<img src={memory.image} ...>`:
    Aggiungere i tre attributi DOPO `referrerPolicy="no-referrer"` e PRIMA della chiusura `/>`.

    **Magic.tsx** — riga 96, `<img src={photo.url} ...>`:
    Aggiungere i tre attributi DOPO `referrerPolicy="no-referrer"` e PRIMA della chiusura `/>`.

    **Secret.tsx** — riga 58, `<img src={photo.url} ...>`:
    Aggiungere i tre attributi DOPO `referrerPolicy="no-referrer"` e PRIMA della chiusura `/>`.

    Non modificare nessun altra parte dei file. Non rimuovere o spostare classi CSS, prop di motion, o altri attributi esistenti.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    Tutti e tre i file compilano senza errori TypeScript. Ogni `&lt;img&gt;` ha `draggable={false}`, `onContextMenu` e `onDragStart` impostati.
  </done>
</task>

<task type="auto">
  <name>Task 2: Aggiungere regola CSS globale anti-drag</name>
  <files>src/index.css</files>
  <action>
    In fondo a `src/index.css` aggiungere il seguente blocco:

    ```css
    /* Prevent casual image saving */
    img {
      user-select: none;
      -webkit-user-select: none;
      -webkit-user-drag: none;
    }
    ```

    Appenderlo alla fine del file esistente, non sostituire nulla.
    Questo agisce come difesa a livello CSS complementare agli handler React.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    `src/index.css` contiene il blocco `img { user-select: none; ... }` alla fine del file.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - Handler `onContextMenu` + `onDragStart` su tutti gli `&lt;img&gt;` in Timeline, Magic, Secret
    - Regola CSS globale `user-select: none` + `-webkit-user-drag: none`
  </what-built>
  <how-to-verify>
    1. Avvia il dev server: `npm run dev`
    2. Apri il sito nel browser
    3. Vai sulla Timeline (pagina Home) → prova click destro su una foto → il menu contestuale NON deve apparire
    4. Prova a trascinare una foto → non deve spostarsi
    5. Vai su `/magic` → ripeti gli stessi test su una foto della galleria
    6. Vai su `/secret` → ripeti gli stessi test
  </how-to-verify>
  <resume-signal>Digita "ok" se tutto funziona, oppure descrivi il problema riscontrato</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passa senza errori dopo entrambi i task
- Tutti gli `<img>` nei 3 file hanno `draggable={false}`, `onContextMenu` e `onDragStart`
- `src/index.css` ha la regola CSS in fondo
</verification>

<success_criteria>
Click destro su qualsiasi immagine del sito non mostra il menu "Salva immagine". Drag & drop delle immagini non funziona. Il sito compila e si avvia correttamente.
</success_criteria>

<output>
Dopo il completamento, crea `.planning/quick/260331-s2e-impedisci-salvataggio-foto-browser/260331-s2e-SUMMARY.md`
</output>
